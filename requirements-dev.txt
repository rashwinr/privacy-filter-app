# Dev / test dependencies — install via:  pip install -r requirements-dev.txt
-r requirements.txt

pytest==8.3.3
pytest-cov==5.0.0
pytest-asyncio==0.24.0
httpx==0.27.2                  # FastAPI TestClient async support
freezegun==1.5.1
